<div class="party-form">
  <h1 class="title" id="playlist-update-title"> UPDATE PLAYLIST</h1>
  <%= form_for @playlist_q do |f| %>
    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %> <br />
    </div>
    <div class="field">
      <%= f.label :description %>
      <%= f.text_field :description %> <br />
    </div>
    <div class="field">
      <%= f.label :theme %>
      <%= f.select(:theme, @themes, {:prompt => "Select a Theme"}, { :class => 'browser-default dropdown' }) %> <br />
    </div>
    <div class="field">
      <%= f.label :guest_song_limit %>
      <%= f.select(:song_limit, @song_limits, {}, { :class => 'browser-default dropdown' }) %> <br />
    </div>


    <button class="btn login-session" id="playlist-edit-button" type="submit" name="action"> Update </button>
</div>


<% end %>
